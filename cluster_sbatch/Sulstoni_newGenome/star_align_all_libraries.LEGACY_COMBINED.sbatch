#!/bin/bash
# LEGACY / DISABLED: combined-library STAR (all FASTQs in one run).
# Use star_align.sbatch (per-library) instead.
echo 'ERROR: star_align_all_libraries is disabled (combined-library). Use star_align.sbatch.' >&2
exit 1
